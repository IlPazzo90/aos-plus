## PASS

- Attaccato: la chiave di dedup dei rifiutati include davvero `lesson_type`, `confidence`, `recommended_action` e `reason` in entrambi i rami SQL → il codice reale in `bin/aos-learning.py:327-339` corrisponde all'artefatto (ramo project e ramo global identici salvo `project`), e i parametri bindati seguono l'ordine dei placeholder in entrambi i rami (8 nel project, 7 nel global). Ha retto.
- Attaccato: il test di regressione riproduce esattamente il caso "pin A / drop A" del MINOR precedente → `tests/test_learning.py:117-121` registra due candidati globali con `recommended_action` contraddittorie, attende `(1, 1)` e due righe in `lessons`; l'invarianza del test precedente (`test_unverified_model_only_claim_is_rejected_and_persisted`, stesso candidato ripetuto → count crescente, una sola riga) resta compatibile con la nuova chiave. Ha retto.
- Attaccato: confronto `reason = ?` con NULL nella colonna `reason TEXT` nullable → `reason` è sempre passato come stringa non nulla dai chiamanti di `_reject` (i test mostrano solo reason testuali come `invalid_evidence`, `not_mechanically_verified`), quindi il confronto `=` non degrada silenziosamente in "nessun match" per le righe rifiutate scritte da questa funzione stessa, che inserisce sempre `reason` valorizzato. Ha retto.
- Attaccato: coerenza tra chiave di dedup e valori inseriti → i campi coerciti (`lesson_type` via `_as_text`, `confidence` normalizzata a `low` se invalida, `recommended` via `_as_text`) sono gli stessi oggetti usati sia nella SELECT che nella INSERT, quindi un secondo candidato identico dopo la normalizzazione trova sempre la riga precedente e non crea duplicati spuri. Ha retto.

Il PASS si applica esclusivamente a questa correzione di `_reject`. Le limitazioni su isolamento e benchmark restano quelle già documentate e non sono oggetto di questo round.
