# Ripresa operativa — 22 settembre 2026

**WIP, NOT READY FOR MAIN.** Entrambe le edizioni restano sul branch
`upgrade/runtime-independent-open-execution`; VERSION 2.4.0. Non mergeare né
pubblicare una release stabile fino alla chiusura dei gate residui.

## Recuperato e verificato

- Baseline privata f68b1cb, pubblica 11542a1; tentativi open precedenti recuperati.
- Routing MID per T2 definito, premium per T3/HIGH/incertezza; host indipendente.
- Open executor/fixer sul vincitore configurato; ruolo/model override effettivo.
- Ledger lesson scoped con check completi, storico conservativo, budget e context handoff.
- Hook Stop validato, regressione in suite; nessuna modifica a hook vendor o permessi globali.
- E2E T1 open-only PASS. E2E planner Claude MID → DeepSeek → Codex MID PASS.
- E2E planner Codex MID → DeepSeek → Claude MID → finding validati → open fixer
  → reverify → arbitrato PASS. Finding ulteriori refutati con prova del file/diff reale.
- Benchmark su task storico valido per Claude Code/OpenCode × DeepSeek/Qwen,
  con worker health separata dai test. Nessuna promozione del vincitore.

## Sicurezza runtime

Claude Code è l'unico runtime open abilitato, con soli tool file. Letture di `.env`
sintetico e symlink esterno negate. OpenCode ha invece restituito entrambi i canary:
è disabilitato nella policy e l'adapter rifiuta gli override espliciti. Codex open
resta disabilitato per la precedente prova fallita delle regole native. Gli adapter
rimangono nel codice; i CLI restano utilizzabili come host/planner/reviewer secondo
le loro autorizzazioni. Non riabilitare un runtime da un semplice test di connettività.

## Evidenze e prossima azione

Leggere OPERATIONAL-VALIDATION.md, operational-evidence.json e REVIEW-LOG.md.
Il database delle lesson è locale e fuori dal worktree; non copiarlo nella distribuzione.
Le tracce grezze e i fixture restano fuori da Git. I risultati quantitativi finali
sono nel verbale, non dedotti da questa nota.

Review mirata Fable completata: finding corretti e ultima correzione con PASS.
Restano: review completa di release; ampliare la matrice
security e il benchmark revisionato; isolamento dei runtime esclusi; misure di costo
complete, capacità MID executor e gestione concorrente dei budget prima di dichiarare
soddisfatti i requisiti completi. Nessun PASS sintetico sostituisce un gate live.

Rollback: revert dei soli commit della ripresa. Le guardie originali e il Security
Gate non sono modificati. Conservare il blocco runtime finché prove nuove non lo
sostituiscono; non ridurre sicurezza per riprodurre i vecchi punteggi del benchmark.
